# Using-class-object-variable
In Python, class object variables are variables that are shared by all instances of a class. They are defined inside the class definition but outside any instance method. 
